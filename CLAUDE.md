# CLAUDE.md

## Project Overview

**ModelicaTesting** (working name — expected to be renamed once the multi-backend abstraction stabilizes) is a Python framework for regression and unit testing of time-dependent system behavior.

**Current state**: a multi-backend regression harness. Discovers tests by scanning `.mo` files and/or reading `test_spec.json`, simulates via Dymola (Python interface by default, batch `.mos` fallback) or FMPy (prebuilt FMUs), and compares results through a composable MetricTree (NRMSE / tube / final-only / range leaves; and / or / k-of-n / warn combinators; multi-baseline via `"against"`). Reports via live dashboard + interactive Plotly HTML + JUnit XML. Reference files use a hybrid schema supporting multiple named baselines (primary + optional `experiment` / `analytical` / `dymola` / ...).

**Forward direction**: [docs/vision.md](docs/vision.md) lays out a six-layer plug-in architecture (Source → Discovery → Backend → Dataset → Metric → MetricTree) enabling additional backends (FMPy / OpenModelica / Julia / Simulink / data-file ingest), additional dataset types (events, spectra, distributions), and composable metrics (AND / OR / weighted / K-of-N). The current Dymola implementation is the first consumer of the abstraction, not its reference model. See [docs/architecture.md](docs/architecture.md) for the layer ↔ code mapping and [docs/extensibility.md](docs/extensibility.md) for plug-in contracts.

**Phase 1 status (complete)**: Foundation abstractions are in place — `Capability` / `DatasetType` enums declared on `DymolaRunner`; `VariableComparison` carries a `diagnostics` bag; `comparison/metric_tree.py` provides `MetricResult` + `And/Or/KOfN/WarnCombinator` + `implicit_and_tree()` (unit-tested, not yet wired into the pipeline); `Config.source_type` reserved; `examples/modelica/ModelicaTestingLib/` relocated; reference-file `Baseline` view supports a hybrid schema for named baselines (primary stays flat; additional baselines live under an optional `baselines` key). Zero runtime-behavior change. Decisions D44–D47 in [docs/decisions.md](docs/decisions.md).

**Phase 2 status (2.1–2.4 complete; 2.5 deferred)**: FMPy backend implemented as the second `SimulatorRunner`, exercising the Phase 1 capability abstraction. Reference-FMUs binaries fetched via `scripts/fetch_reference_fmus.py` (release-ZIP download, not submodule) into gitignored `examples/fmu/reference-fmus-binaries/`. `FmpyRunner` (`simulators/fmpy/runner.py`) simulates via `fmpy.simulate_fmu`, persists results to `<test_dir>/result.npz`, and reads back through the existing comparator contract. `test_spec.json` gained an optional `"fmu"` field; `BatchManifest.mat_file` (dead Dymola-specific code) removed from backend-agnostic base. **2.4 done**: end-to-end CLI works against `examples/fmu/testing.json` (BouncingBall, Dahlquist, VanDerPol) — `Config.__post_init__` and `discover_tests` skip Modelica package.mo lookup when `source_type == "fmu"`; `library_name` falls back to the config dir name. Baselines committed under `examples/fmu/ReferenceResults/FMPy/<os>/`. **2.5 deferred**: GitHub Actions CI until the repo goes public; full recipe + decisions in [docs/PHASE_2_5_CI_PLAN.md](docs/PHASE_2_5_CI_PLAN.md). Decisions D48–D50 in [docs/decisions.md](docs/decisions.md).

**Phase 3 status (complete)**: MetricTree wired end-to-end. `comparator.compare_test()` always builds a `MetricResult` tree and derives `TestComparison.passed` from its root. Default tree is the implicit flat-AND `implicit_and_tree(comparisons)` (behavior-preserving). Users author explicit trees via a new `"metrics"` block in `test_spec.json`: parsed by `comparison/tree_spec.py` into `LeafSpec` / `CombinatorSpec` with path-bearing validation, evaluated by `comparison/tree_eval.py` against sim + reference data. Combinators: `and`, `or`, `k-of-n` (with `k`), `warn` (single child). Leaf metrics: `nrmse`, `tube`, `final-only`, `range` (last one is signal-only — bounds from spec, not baseline — proves the leaf contract isn't NRMSE-shaped). Per-test HTML report renders user-authored trees via recursive Jinja; implicit trees stay suppressed (per-variable table already conveys them). Decisions D51–D53.

**Cleanup pass (complete)**: diagnostic variables stored as scalar summary (`final/min/max`) not full trajectory — kills spurious git diffs on every `--accept`; reporter's "NRMSE" column → mode-aware "Score" (`NRMSE 2.1e-16`, `max_viol 0`, `100% in tube`); backend-aware artifact lists (`DymolaRunner.artifact_files` vs. `FmpyRunner.artifact_files`); documented per-test `simulation.timeout` (honored by Dymola today, not FMPy). Also fixed a bug where `discover_tests` silently dropped `test_spec.simulation.timeout` and `test_spec.metrics` when a model had both a `UnitTests` component and a spec entry. Decisions D54–D55.

**Phase 4.A status (complete)**: Multi-baseline MetricTree leaves. Leaves select via `"against": "<name>"` (default `"primary"`). `comparator.compare_test` loads every named baseline from the reference file via `_extract_baselines(reference)` and threads them into the evaluator as `baselines: dict[str, BaselineView]`. New `ReferenceStore.add_named_baseline(model_id, name, ...)` helper for programmatic non-primary baseline authoring (primary stays owned by `store_reference`). Per-test HTML shows `against=<name>` on non-primary leaves. BouncingBall demo has a synthetic `experiment` baseline + a `warn`-wrapped leaf scoring against it. Test count: 253 → 309. Decision D56.

**Phase 4.D status (complete)**: Modelica-neutral rename sweep + bundled cleanup follow-ups. Hard break: `TestModel.mo_file` → `source_file`, `TestModel.package_path` → `source_package`, `Config.package_path` → `Config.source_path` (CLI `--source-path`, `testing.json` key `source_path`). Modelica-specific code paths (`mo_parser`, `find_package_dir`, `dymola/`, ...) keep their Modelica names — they're genuinely Modelica-only. Bundled into the same phase: FMPy now honors `test.timeout` / `config.timeout` via `concurrent.futures` (worker thread leaks on timeout — acceptable for sub-second FMUs since C-level FMU compute can't be force-killed in-process); FMPy emits `loading` / `simulating` phase labels for dashboard parity with Dymola's `translating / simulating / finalizing`. External `testing.json` consumers (TRANSFORM) need a one-line key rename. Test count unchanged: 309. Decisions D57–D58.

**Phase 5 / PTA status (complete)**: Pluggable in-source test annotations. The Modelica `.mo` scan was generalized into a `Recognizer` registry (`discovery/recognizer.py`); the previous hardcoded `UnitTests` + `experiment(...)` pattern is now `BundledModelicaUnitTestsRecognizer`, one of N registered. Users declare custom recognizers as JSON in `testing.json` (`"recognizers"` list) — `JsonRecognizer` (`discovery/json_recognizer.py`) handles both `component-instantiation` and `extends` matchers, with `parameter` / `constant` / `experiment-annotation` field sources. Discovery merges results by `model_id`; bundled registers first, user appends, last-writer-wins per field — additive default + explicit `disable_bundled` opt-out. `TestModel` gained richer-contract fields (`simulate_only`, `requested_fmu_export`, `requested_baselines`); `simulate_only` is wired end-to-end in the comparator (test passes iff sim succeeds; no per-variable comparison; the others are 4.B placeholders). Demo lives in `examples/modelica/ModelicaTestingLib`: new `Icons/Example.mo` + `Examples/SimulateOnlyTest.mo` exercised by a recognizer entry in the bundled `testing.json`. Decomposition: PTA.1 registry → PTA.2 JSON schema → PTA.3 wire-into-Config → PTA.4 richer fields → PTA.5 simulate_only end-to-end → PTA.6 demo → PTA.7 docs+D59. Test count: 309 → 358. Decision D59.

**Bundled phase: PTA follow-ups + 4.E + 4.C + 4.B + interactive HTML (complete)**: Five originally-separate moves landed in one session.
- **PTA follow-ups (D60)**: `paths_include`/`paths_exclude` per-recognizer (folder filter); `all-of`/`any-of` match composition (recursive `CompositeMatch`); `class-name-glob` match type; `annotation` field source (extracts from any Modelica annotation block, not just `experiment(...)`).
- **4.E weighted combinator (D61)**: `WeightedCombinator` joins and/or/k-of-n/warn — `sum(w_i * score_i) <` (or `>`) `threshold`. Direction-aware so it works for NRMSE-shaped (lower better) and tube-shaped (higher better) trees.
- **4.C leaf metrics (D62)**: `event-timing` (compare event instants via duplicate-time detection; `time_tolerance`); `dominant-frequency` (FFT peak comparison; `rel_tolerance`). Same `ComparisonMode` plumbing as `range` (D53).
- **4.B cross-backend (D63)**: `Capability.FMU_EXPORT` is now real; `DymolaWorker.export_fmu` uses `translateModelFMU`; `simulators/cross_backend.py::produce_dymola_via_fmpy_baseline` chains primary export → FMPy simulate → named baseline `"dymola-via-fmpy"`. CLI's `cmd_run` invokes the chain after primary `run_tests` for tests with `requested_baselines=["dymola-via-fmpy"]` (PTA.4 field). **Validation caveat**: real Dymola export requires Windows + Dymola FMI license; tests use mocked `DymolaInterface` for the export step (FMPy half is real).
- **Interactive HTML (D64)**: NRMSE-only column → mode-aware `score_display`; per-variable tolerance input shows `n/a (mode=...)` for non-NRMSE modes; JS `computePass` special-cases non-NRMSE modes to use the originally-computed `v.passed`. Surgical fix preserves NRMSE slider workflow.

Test count: 358 → 404. Decisions D60–D64.

**Paper + Phase-6-plan close-out (docs-only)**: AMC 2026 paper scaffolding drafted in parallel at `/mnt/d/Papers/amc2026_testing/` (DOMAIN.md, PROJECT.md, CODE_PROJECT.md, SNAPSHOT.md, experimental_log.md, starter `.bib`, 7 TikZ/tabular figures + 1 screenshot placeholder). `docs/PHASE_6_PLAN.md` added as the implementation-oriented decomposition of Phase 6 MVP (6.0 perf budget + 6.1 per-leaf panels + 6.4 full-fidelity spec-update, ~3–4 weeks) for the first post-paper session. SESSION_HANDOFF.md starter prompt refreshed to land directly on the MVP. No code changes. Test count unchanged: 404.

**Phase 6-9 design grill (complete, docs-only)**: D66. Eight-branch grilling session stress-tested the forward roadmap and captured six structural commitments: (1) tool identity is **regression testing, period** — V&V against experiment is handled by composing instances of the tool rather than a separate test shape; (2) **baseline roles split into three**: primary (stored sim, regression anchor, validator-required outside `warn`), companion references (file-path pointers, visual-only overlays, graceful load-failure degradation), soft_checks (cross-regression imports, warn-wrapped scoring only, never hard-fail); (3) **reporter-as-IDE** — interactive HTML is the primary authoring surface, CLI is the execution surface, download RFC 6902 JSON-Patch + `spec-update` read-modify-write round-trip, vanilla JS in browser, Config-dataclass-derived UI with override slots; (4) **Phase 6 MVP = 6.1 + 6.4 (~3-4 weeks)** with live preview for simple modes only; (5) **recommender is rule-based only** with bounded feature vocabulary + hard input/output contracts + complexity budget — **Phase 8 (ML) removed from scope**; (6) **reporter testing = Python-exhaustive + golden-file HTML snapshots + markdown QA checklist** (no JS framework, no E2E yet). Economy-of-tools principle named: we do one thing well and emit handoff-ready artifacts; calibration/RCA/parameter-estimation/ML-ranking belong in downstream tools that consume our outputs. Docs updated: `docs/vision.md` (baseline-role model, scope IN/OUT with rejection criteria, reporter-as-IDE section, recommender containment section, principles 6-9); `docs/decisions.md` (D66 full entry). No code changes this pass. Test count unchanged: 404. Decision D66.

**Phase 6.1.2 / 6.1.3 / 6.1.5 — JS scorer ports + per-mode panels wired into interactive.html (complete)**: Finishes the D66 reporter-as-IDE step so every mode actually shows a working panel in the browser. Auto-derived panels from 6.1.1 now render inline where `n/a (mode=...)` used to sit; vanilla-JS scorer registry (`MODE_SCORERS`) handles live pass/fail recompute for the four simple modes (nrmse, tube, range, final_only); `event-timing` and `dominant-frequency` render their panels with a "CLI-authoritative" badge and fall through to the stored `v.passed`.
- **Python side**: `plot_comparison._build_template_context` now attaches `mode_controls_html`, `mode_values`, and `cli_authoritative` per variable. New helpers `_extract_mode_values(vc, mode)` and `_render_mode_controls(...)` bridge `VariableComparison` / diagnostics into the 6.1.1 UI. `_compare_range` now stashes `min_value` / `max_value` in its `diagnostics` bag so range panels pre-fill the declared bounds.
- **Template**: `interactive.html` variable-table cell branch gained a panel path: nrmse/final_only keep the existing slider, other modes render the auto-derived panel + optional CLI-authoritative badge; tube retains its dedicated editor below. New CSS for `.mode-controls` and `.cli-authoritative`.
- **JS**: `const MODE_SCORERS = {nrmse, tube, range, final_only}` — each `(v, cfg, idx) => {passed, score, label}`. `computePass` refactored to use the registry; falls back to `v.passed` when no scorer is registered (event-timing / dominant-frequency). New `wireModeControls` + `onModeFieldChange` hook panel inputs (`[data-field]`) to `perVarModeConfigs[idx]` and trigger `updateVarStatus` on edit.
- **Tests**: `tests/test_interactive_html_snapshot.py` (8 — structural-hash golden-file snapshot per mode plus per-mode panel-signature presence plus scorer-registry presence; refresh via `UPDATE_GOLDEN=1`). Golden hashes committed under `tests/golden/interactive_*.hash`. Per D66 Q8 — no JS unit framework, no Playwright E2E.
- **Smoke test**: `examples/fmu/` BouncingBall regenerates; `h` variable with `range` metric shows `min_value=-0.01`, `max_value=1.1` pre-filled. Tube editor + NRMSE slider untouched.
- **Still deferred**: 6.4 RFC 6902 JSON-Patch export + schema export + validator wiring at patch-apply time. Test count: 496 → 504.

**Phase 6.4 — full-fidelity `spec-update` round-trip + schema export + QA checklist (complete)**: Closes the Phase 6 MVP authoring loop. Reporter emits RFC 6902 JSON-Patch against the matched test entry; `cmd_spec_update` auto-detects patch vs. legacy dict, dry-run applies, runs the D66 validator if the entry has a metric tree, then commits — partial writes impossible. Unknown keys + hand-authored `description` / `info` / `metadata` survive byte-compat.
- **`discovery/patch_apply.py`** *(new)*: `apply_patch(spec_path, model_id, patch_ops, whitelist)` — pure-stdlib RFC 6902 applier, RFC 6901 JSON-Pointer escapes (`~1` → `/`, `~0` → `~`), whitelist defaults to `/comparison` + `/metrics`, supports `replace` / `add` / `remove`. Raises `PatchError` with path on any failure. Creates missing test entries on `add`. Self-contained — no new deps.
- **`cli.py::cmd_spec_update`**: auto-detects `update_data["patch"]` shape; new path stages (a) dry-run apply to in-memory dict (catches path/whitelist issues) → (b) re-parse + validate any mutated metric tree via `comparison/validator.py::role_lookup_from_store` → (c) commit. Legacy flat-dict format preserved for a transition cycle.
- **Reporter JS**: new `buildPatchData()` diffs current vs. original (`perVarTolerances[i]`, `perVarModeConfigs[i]` vs. `v.tolerance_used`, `v.mode_values`); emits `{"model", "patch": [ops]}`. Helper `jsonPointerEscape(s)` handles `/` and `~` in variable names. `buildExportData` retained as an alias for any downstream. Download filename: `spec_patch.json`; copy text is now a JSON-Patch envelope.
- **`reporting/schema_export.py`** *(new)* + **`cmd_export_schema`**: derives a JSON-Schema draft 2020-12 document describing `test_spec.json` from the Config dataclasses + MetricTree grammar. `$defs` contains per-mode entries (reuses 6.1.1 introspection for field types, Literal choices, Optional markers) plus `leaf` / `combinator` / `tree_node`. Emits to stdout by default; `--output` writes to a file. Useful downstream: IDE autocomplete, LLM-authored specs, handoff artifacts.
- **Tests** *(+27)*: `test_patch_apply.py` (17 — basic ops, unknown-key preservation across entry / comparison / sibling-tests, whitelist enforcement, JSON-Pointer escape rules, error surfacing, missing-model auto-append), `test_spec_update_cli.py` (3 — end-to-end patch application, whitelist rejection nonzero exit + spec byte-preservation, legacy dict still honored), `test_export_schema.py` (7 — builder contract, mode defs present, Literal choices emitted, CLI writes to stdout/file).
- **`docs/qa/reporter_checklist.md`** *(new)*: D66 Q8 manual click-through checklist for pre-release QA. Covers per-mode UX, patch round-trip, schema export, baseline-role CLIs, and known failure modes.
- **Test count**: 504 → 531.

**Phase 6 MVP checkpoint (all steps complete)**: 6.0 (payload budget) + baseline-role split (D66 storage + CLI + validator) + 6.1.1 (auto-derive + bundled #46 time-windowed leaves) + 6.1.2/3/5 (JS scorers + cell-replacement) + 6.1.4 (tube cell-link + range plot overlay) + 6.4.1-6 (patch round-trip + schema + QA). Deferred by design: 6.1.4 drag-to-edit range handles (v2); 6.1.1 window UI surfacing on every panel (auto-derive currently reads LeafSpec.window_* but doesn't yet render it); JS unit framework; Playwright E2E. Captured as-built in **D67**; `docs/PHASE_6_PLAN.md` retired (git history preserves the working plan). Post-MVP follow-ups tracked in `docs/ideas.md` (#47–#49 payload tier-2/3, window UI surfacing, drag-to-edit range handles); Phase 7 rule-based recommender next.

**A-tier MVP close-out (complete)**: Both half-shipped MVP follow-ons landed in one session. (1) **Window UI on auto-derived panels** (idea #46 UI surfacing): `reporting/ui/mode_controls.py::render_window_controls_html` emits universal `window_start` / `window_end` inputs as a standalone fragment (window lives on `LeafSpec`, not any `ModeConfig`, so it's not threaded through per-mode schemas); new `comparison/tree_spec.py::collect_leaf_paths` walks spec nodes in pre-order producing JSON-Pointer paths aligned with `collect_leaf_variables`. Per-variable `leaf_path` + `window_values` threaded through `_build_template_context`; tree-backed leaves get the window UI, flat-override leaves suppress it. Interactive template wires `perVarWindows[idx]` via `wireWindowControls`; `buildPatchData` emits RFC 6902 `add` / `remove` ops at `<leaf_path>/window` — whitelist already covers `/metrics`. (2) **Companion + soft_check overlay rendering** (idea #50, 6.3 first slice): new `reporting/overlay_loader.py` loads registered overlays via `store.get_soft_checks()` + `store.get_companions()`; JSON + wide-CSV formats; every failure mode (missing file, bad parse, broken store) absorbed into `Overlay.status` (`loaded` / `missing` / `invalid`) with a human-readable `note`. Per-plot picker (default-off checkboxes) toggles traces via `Plotly.restyle(visible: 'legendonly' ↔ true)`; test-level `<details>` summary lists every overlay including unrendered ones. Soft_checks render purple dotted, companions green dashdot; overlay trajectories share the `max_embedded_samples` LTTB cap. Tests: `test_mode_controls.py` (+5 window rendering), `test_window.py` (+5 leaf-paths class), `test_patch_apply.py` (+4 window path class), `test_overlay_loader.py` (17 new). Test count: **531 → 562**. The MVP is now visually complete — every backend feature has a browser authoring surface.

**Reporter-as-recursive-tree refactor (complete, 5 stages, one session)**: D68-pending. Rebuilt `interactive.html` + `plot_comparison.py` around a single recursive `SpecNodeView` JS component. **Stage 1** — pure-Python primitives: `tree_spec.collect_variables`, `leaves_for_variable`, `synthesize_implicit_tree`, `spec_to_view`; `tree_eval.flatten_evaluation`; `ModeUI.plot_contribution` slot + `PlotContribution` dataclass; `emit_mode_schemas()`. **Stage 2** — one plot per unique variable (trajectories deduped); recursive JS component (`renderNode` / `renderLeaf` / `renderCombinator` filtered by `variableFilter`); path-keyed `leafState` replaces per-leaf-index JS arrays; `MODE_PLOT_CONTRIBUTIONS` registry (range dashed lines, tube polygon, final-only marker, window x-band highlight). Stripped to match user's "reduce features" directive: rich Shift+click/drag tube editor (critical — must reimplement, see `memory/project_tube_editor_critical.md`), error-overlay dropdown, global tolerance slider, mode-select dropdown, per-plot error panels. Tube's `custom_renderer` retired; schema-driven inputs now edit tube config. **Stage 3** — full-tree mount at top of report (same recursive component, unfiltered), `syncSiblingInputs` propagates edits between full-tree + per-variable mounts, retired `metric_tree_view`. **Stage 4** — `+` / `−` buttons on tree nodes, `WORKING_TREE` deep clone tracks structural edits, `nodeToSpec(node)` strips render artifacts, structural `buildPatchData` emits a single `{op: "add", path: "/metrics"}` wholesale replacement. **Stage 5** — deleted static `comparison.html` template + its renderer, deleted matplotlib PNG generation loops + `_plot_variable` (~90 lines), deleted flat `variables` list + `diagnostic_plots` / `compared_plots` / `nobaseline_plots` context fields (~90 more lines), retired `html as html_mod` import. Tests: `test_window.py` (+8 collect_variables / leaves_for_variable / synthesize / spec_to_view / flatten_evaluation classes), `test_mode_controls.py` (+4 PlotContribution + emit_mode_schemas), `test_patch_apply.py` (+3 wholesale `/metrics` replace), `test_interactive_html_snapshot.py` rewritten against per-variable layout. Test count: **562 → 595**.

**Reporter polish + interactive plot editors + browser tests (complete, 2026-04-21)**: The refactor's aftermath — what started as "bring back the tube editor" grew into a structural pass. **MODE_PLOT_EDITORS** general contract (activate/deactivate + event cleanup) — three consumers: tube (full v2 reimplementation: auto-seed, table with synced/unsynced/per-side modes, widthMode selector, interpolation, min-width, Shift+click/drag/right-click on plot, live pass/fail), range drag (`edits.shapePosition` via `plotly_relayout` filter on shape name), window brush (universal `🔲 Set window from plot` button driving Plotly horizontal selection). **MODE_SCORERS** live pass/fail recompute for nrmse/tube/range/final-only — AND/OR/warn/k-of-n bubble-up honours combinator semantics. **Error overlay dropdown** per plot (none / signed / abs / NRMSE) on right y-axis. **Unified activate/deactivate lifecycle** — `.node-editor` slot clearing centralized in core, editors only own event-handler cleanup; idempotent across re-activations. **Per-leaf tube editor refresh** — all state mutations refresh every mounted slot via `refreshTables(leaf, commit)` (fixes the "edit in one mount, stale in another" bug). **In-place pt mutation + reference-tracked drag** — `setPoints` clones only for structural changes; drag tracks `drag.pt` reference + re-derives index via indexOf; smooth swap when dragging past another point. **Zoom preservation** — `Plotly.react` replaces `newPlot` after first paint + `uirevision: 'keep'` on axes. **JS extracted** from `interactive.html` into standalone `interactive.js` (~1700 lines pure-static JS, no Jinja); HTML marshals per-report data into `window.MT_REPORT` + loads JS via `<script src>`. **Plotly CDN swap** (`cdn.plot.ly` → `cdnjs.cloudflare.com`, reachable from WSL + Playwright). **Variable picker v2** in `+` modal — `<input list>` + `<datalist>` + glob support + untracked-name warning. **Inline remove-confirm popup** replaces `window.confirm` (Confirm / Cancel buttons, click-away + ESC dismiss). **Playwright browser-driven tests** — 35 tests in `tests/test_interactive_playwright.py` covering every interactive flow, `pytest.importorskip` gates the module on systems without Playwright. **Tube's schema inputs suppressed** for tube leaves (editor owns the slot); **tube control-point markers hidden from legend** (`showlegend: false`). **Icons.Example discovery bug fixed** — JSON recognizer's `extends` + `component-instantiation` matchers now strip string literals + comments before regex scan, so prose inside a Documentation annotation no longer triggers false-positive recognition. **TREE_VIEW vs WORKING_TREE bug fixed** — introduced `currentTree()` so structural edits show up in both tree renders AND plot contributions. Test count: **595 → 637** (+42). Interactive reporter is now feature-complete for the Phase 6 MVP authoring-surface story.

**Phase 6.1.4 — custom mode-UI overrides (complete)**: Tube cell + range plot overlay — modest addendum to 6.1.2/3/5.
- **Tube**: The auto-derived cell panel was redundant with the rich tube editor below the plot. Registered a `_tube_cell_renderer` as tube's `custom_renderer`, so the variable-table cell now emits `"→ See tube editor below plot"` instead of a duplicate inputs block. Rich editor unchanged.
- **Range**: The auto-derived cell's `min_value` / `max_value` inputs now drive a pair of dashed-red horizontal reference lines on that variable's trajectory plot. New JS `applyRangeOverlay(idx)` builds `layout.shapes` and `Plotly.relayout`s the plot. Called from `renderPlots` when mode=range, and re-applied inside `onModeFieldChange` when either bound input changes. v1 is input-driven (input → plot); drag-to-edit (plot → input) stays deferred.
- **Tests**: refreshed six golden structural hashes (tube's cell content shrank; range's snapshot unchanged from my existing fixture's input-fill state). The panel-signature test now looks for `"See tube editor below plot"` on tube. Test count unchanged: 504.

**Phase 6.1.1 — auto-derive UI machinery + time-windowed leaves (complete)**: Six-mode UI auto-derivation + idea #46 bundled per the 6.1.1 checkpoint criterion.
- **Mode-controls module** (`reporting/ui/mode_controls.py`): introspection walks each `ComparisonMode`'s typed Config dataclass via `typing.get_type_hints` + `get_origin`/`get_args`, producing a `Schema` of typed `FieldSpec`s. Handles float / int / bool / str, `Optional[X]`, `Literal[...]`, `Optional[Literal[...]]`; complex types (`list[dict]`, nested dataclasses, forward refs) degrade to a `passthrough` raw-JSON textarea without crashing. Vanilla-HTML renderer (`render_schema_html`) emits a `<div class="mode-controls" data-mode="..." data-variable="...">` fragment with labeled inputs (number/checkbox/select/text) + optional-enum unset slot + HTML-escaped variable names. Registry keyed by mode name (`register_mode_ui`, `get_mode_ui`); six bundled modes auto-register at import. Custom-renderer slot preserved for 6.1.4 (tube conditional fields, range visual handles). `ComparisonMode` ABC stays pure compute (D66 Q2) — the UI bridge lives here.
- **ModeConfig typing tightening**: `TubeConfig.tube_width_mode` → `Optional[Literal["band", "rel", "absolute"]]`; `TubeConfig.tube_interpolation` → `Literal["linear", "constant"]`. Zero runtime-behavior change; feeds the auto-derive dropdown choices.
- **Idea #46 bundled (time-windowed leaves)**: at the 6.1.1 checkpoint criterion — "shared cross-mode subschema without leaking into each mode's Config" — window lives on `LeafSpec` (not on any ModeConfig), so the checkpoint passes. New `LeafSpec.window_start` / `window_end: Optional[float]`; parsed from JSON under a `"window": {"start": float, "end": float}` key (either endpoint optional; `end > start` enforced; malformed shape rejected). `tree_eval._evaluate_leaf` gains a `_slice_window` helper that restricts both `ref_*` and `act_*` arrays to `[start, end]` *before* `mode.compare` — modes stay untouched. Windowed leaves record `diagnostics.window`. Piecewise regression contracts (NRMSE on `[0, 5]` AND NRMSE on `[5, 10]`) compose naturally through the existing combinator grammar — no new combinator needed.
- **Not yet wired into `interactive.html`**: per the PHASE_6_PLAN sequencing, the `n/a (mode=...)` cell replacement is 6.1.5. This step lands the machinery; 6.1.2 ports JS recompute for the four simple modes; 6.1.5 swaps in the panels.
- Tests: `tests/test_mode_controls.py` (24 — introspection per mode, Literal choices extracted, complex-type passthrough, renderer output shape, registry lookup, custom-renderer override), `tests/test_window.py` (13 — parsing edges, evaluation semantics, piecewise composition). Test count: 459 → 496.

**Phase 6.0 — payload budget + baseline-role split (complete)**: Ship landed in two bundled sub-phases.
- **6.0 performance budget**: LTTB decimation at 1000 samples/var embedded into `interactive.html`; `comparison_data.json` sidecar preserved at full resolution as the Tier-2 data artifact (pass/fail scoring + stored baselines untouched). `Config.max_embedded_samples` config field. New modules `reporting/decimate.py` + tests. Default cap is 1000 (not PHASE_6_PLAN's suggested 2000) because each variable embeds 4 arrays (`act_time/act_values/ref_time/ref_values`) — time-array dedup (idea #47) is needed before the cap can safely rise. Three follow-up ideas queued in `docs/ideas.md`: #47 dedup, #48 lazy-fetch, #49 per-test override.
- **Baseline-role split**: D66's flat-named-baselines → three distinct roles now real in code. Primary (unchanged at `ref_NNNN.json`), soft_checks (at `soft_checks/ref_NNNN/<name>.json`, warn-wrapped scoring only), companions (at `companions/ref_NNNN/<name>.{json,csv}`, plot-only overlays). New `Companion` dataclass; new `ReferenceStore` methods (`get/add/remove_soft_check`, `get/add/freeze/remove_companion`); `get_baselines` now returns primary + soft_checks (companions deliberately excluded — not scorable). New `comparison/validator.py` enforces D66's three rules: tree must have ≥ 1 primary leaf outside warn; soft_check leaves must have a warn ancestor; companion targeting is rejected outright. New CLI commands: `companion add/list/freeze/remove`, `soft-check list/remove`, `import-baseline`, `migrate-baselines`. `cross_backend.py` rewired to `add_soft_check`. Legacy `add_named_baseline` removed (no backward-compat shim per repo principle). One-off migration applied to `examples/fmu/ReferenceResults/FMPy/linux/ref_0001.json` (BouncingBall's `experiment` → soft_check); end-to-end `run` still 3/3 pass. Test count: 404 → 459 (+55 across decimate/size-budget/baseline-roles/validator/migration). Decision D67 pending a docs pass.

**FMU-pathway scope + cross-backend experimental labeling (complete)**: D65. Grilled the D63 deferred-validation caveat and surfaced a broader concern: the cross-backend chain (`produce_dymola_via_fmpy_baseline`) assumes Dymola-exports-FMU + FMPy-default-simulates is a meaningful cross-check, which is only true for *autonomous* tests (no external inputs, no python-driver, CS-vs-ME-irrelevant). Same semantic gap exists on the primary FMPy path. Changes: (a) `simulators/cross_backend.py` module docstring flagged **EXPERIMENTAL** with scope limits spelled out; one-time warning emitted by `cli._run_cross_backend_chains` when any chain fires; (b) `simulators/fmpy/runner.py` gained a **Limitations** block documenting what the current `simulate_fmu` call does *not* support (`input=` / `fmi_type=` / `start_values=` / python-driver tests) — scope-labeling only, *not* a Phase 2 status reversal (reference FMUs remain validated); (c) new `scripts/smoke_test_dymola_export.py` — standalone script the user ran on Windows to validate `translateModelFMU` signature + FMI license + cwd-on-Windows, independently of the test suite. **Smoke test PASSED on Dymola 2026x (2026-04-17)** against `Modelica.Blocks.Examples.PID_Controller` — signature/license/cwd dimensions of the validation caveat are now locked. Full chain (export → FMPy-simulate → baseline write) still unvalidated on real Dymola; semantic-gap generalization (input drivers, CS/ME choice, start-value overrides, python-driver tests) bundled into a future "FMU-path semantic gap closure" phase. No runtime-behavior change beyond the warning. Test count unchanged: 404. Decision D65.

**JuliaMtkTestingLib full library structure + 5 new ports + observables fix (complete, 2026-04-23)**: D79. Reorganized `examples/julia/` into `JuliaMtkTestingLib/` mirroring ModelicaTestingLib's layout (Examples/*.jl, Resources/ReferenceResults/). Ported 5 more tests matching Modelica counterparts: Constant, MultiFrequency, RangeCheck, TubeTolerance, MetricTree (plus existing SimpleRamp + Frequency). Scope-(a) per user: test-fixture demo, NOT publishable Julia package — no `src/JuliaMtkTestingLib.jl` module file. Test count 2→7; 7/7 PASS on self-regression. **Driver bug fix**: `structural_simplify` classifies algebraic outputs (e.g., `y ~ a1*x1 + ...`) as OBSERVABLES not unknowns; original driver only read `unknowns(sys)` so `y` was silently missing from MultiFrequency's result. Both `run_test.jl` and `run_persistent.jl` now collect both unknowns AND `observed(sys).lhs`, materializing via `sol[expr]`. Skips MTK-internal helpers (`ˍ`-prefixed) and unmaterializable expressions. **14 cross-library companions re-wired** after the move (7 Julia→Modelica + 7 Modelica→Julia), portable relative paths. Browser-verified: Julia MultiFrequency shows `modelica-om-multifrequency` overlay (502 time points). Deferred: EventTest (MTK callbacks vs Modelica `when`), IntervalTest (Modelica-specific), NoUnitTest (Modelica fixture), SimulateOnlyTest (need spec_parser wiring). Test count: 752 → 752 (unchanged; 3 integration tests cover the expanded library via smoke flow). Decision D79.

**Persistent-worker Julia + cross-library companions (complete, 2026-04-23)**: D78. Two follow-ons to D77. (1) **PersistentJuliaRunner** — long-lived `julia --project=... run_persistent.jl` subprocess reading JSON-per-line test requests from stdin, writing JSON responses to stdout. `using MTK, OrdinaryDiffEq, JSON3` paid ONCE per worker at startup (~15-40s cache read), not per test. Warmup phase: workers emit `{"event": "ready", "pid": ...}` pulse once `using` completes; dispatcher blocks on the pulse before accepting tests, so first-test wall-time reflects only compile/solve. `Base.invokelatest(build_mtk_system)` after `include` forces fresh definition binding. Mirror Dymola/OM's shared-queue + dispatch-thread-per-worker + psutil kill + 3× restart pattern. **Validation**: examples/julia 2-test suite — batch 39s, persistent 24s (11.5s warmup + 12.5s in-session); first-test eats per-model JIT, subsequent tests ~1s. Scales with N. (2) **Cross-library companions** wired ModelicaTestingLib ↔ examples/julia both directions (4 refs total) via the existing companion-path machinery with portable relative paths — no new plumbing needed (add_companion stores strings verbatim, loader resolves relative against ref_dir). Sibling vs companion clarified: same-code-different-simulator = sibling (auto-discovered), different-implementation = companion (manually wired). Julia tests use companion mechanism to overlay against Modelica baselines. End-to-end verified: Modelica FrequencyTest report shows julia-mtk-frequency overlay (119 time points, role=companion) alongside Dymola/windows sibling. Decision D78.

**Julia / ModelingToolkit backend (complete, 2026-04-23)**: D77. Fourth `SimulatorRunner` alongside Dymola + FMPy + OpenModelica. First non-Modelica-syntax source — proves the framework's simulator-neutral abstraction. User writes `.jl` files exporting `build_mtk_system()` → `(sys, u0, ps)`. Framework driver (`run_test.jl`) invokes one Julia subprocess per test, captures result as JSON. `JuliaRunner` with `BATCH_FALLBACK` capability. `examples/julia/` self-contained demo with pinned MTK 11.22 + OrdinaryDiffEq 6.111 + JSON3; two sample tests (SimpleRamp, Frequency). End-to-end: `modelica-testing run --config examples/julia/testing.json` → 2/2 PASS; CLI passed == live JS scorer passed (D76 bin-aligned FFT works unchanged). +3 gated integration tests (`julia` marker). Dyad deferred (compiles to MTK, likely works). MTK FMU export deferred. Test count: 749 → 752. Decision D77.

**Live JS FFT + window-scoped detection + per-peak provenance (complete, 2026-04-23)**: D76. Closes the Q1 loop from D75. Ports `_compute_fft_spectrum` + `_find_top_n_peaks` + `_find_strongest_peak_in_window` to JS (~190 lines — radix-2 Cooley-Tukey with zero-pad-to-next-pow-2). Spectrum subplot now recomputes live on window edit; `getLiveSpectrum(leaf, source)` slices the trajectory by `leafState.window` and FFTs. Detect button gets a `Source: [Reference | Actual]` dropdown and uses the live windowed spectrum of the selected source — enables the "brush a window, detect a peak, pin it, brush another, repeat" workflow. Each seeded peak carries `derived_from_window: {start, end}` provenance metadata (rounds-trips through patch). Live match column in the table replaces CLI-frozen paired_peaks. Live JS scorer ports to live FFT too. Multi-window scoring kept outside the leaf — achievable via metric-tree composition (two dominant-frequency leaves with different windows under AND). End-to-end verified on MultiFrequencyTest: window [1,3] + Detect-from-Actual detects peaks near [1, 3, 7] Hz (FFT bin resolution permitting) with derived_from_window stamps. +2 Playwright tests. Test count: 746 → 749. Decision D76.

**Declared-peaks dominant-frequency + PointPlotEditor abstraction (complete, 2026-04-23)**: D75. Replaces D74's top-N-by-amplitude algorithm with a declarative table of expected frequencies: `peaks: [{freq, tolerance, tolerance_mode}, ...]` with per-row `rel`/`abs` tolerance. Algorithm: for each declared peak, find the strongest local maximum in the actual spectrum within `[f*(1-tol), f*(1+tol)]` (rel) or `[f-tol, f+tol]` (abs); leaf passes iff every declared peak has a match. Hard-fail with `"no peak in tolerance window"` on unmatched peaks; fail-with-hint on empty `peaks` list. Clean break — `rel_tolerance`/`min_frequency`/`n_peaks` config fields removed. **New `createPointPlotEditor` factory** (~140 lines) extracts the Shift+click/drag/right-click interaction from the tube editor into a reusable abstraction consumers can parameterize via `{getAnchors, onClickAdd, onDragStep, onDragEnd, onRemove}` callbacks; anchors track `pt` refs so consumers can sort their arrays mid-drag without losing the drag target. **Tube migrated to the factory** (behavior-preserving — all 9 tube tests still green). **Declared-peaks editor v2**: spectrum subplot + declared-peak diamond markers colored by CLI pass/fail + shaded acceptance bands + Shift-interactivity + editable table with `+ add peak` and `🔍 Detect peaks from reference` buttons (detect wraps the old top-N algorithm as a bootstrap helper on fresh tests). **Live JS scorer** for dominant-frequency — no longer CLI-authoritative (event-timing still is). `FrequencyTest` + `MultiFrequencyTest` specs rewritten to declared-peaks format; baselines re-accepted. +2 Playwright tests; 6 old multi-peak Python tests replaced with 8 new declared-peaks tests. End-to-end verified: spectrum mounts, table renders 3 peaks matching `[1, 3, 7]` Hz, Detect button populates from reference spectrum's top-3. Test count: 744 → 746 (+2 net). Decision D75.

**Leaf-mode UX pass: visibility sync, labels/help, multi-peak frequency, event-timing + spectrum plots (complete, 2026-04-23)**: D74. Follow-on from a user UX review of the six leaf modes. Two PRs bundled in one session. **Phase 1 — labels/help/sync**: (a) every `ModeConfig` dataclass field now carries `metadata={"label": ..., "help": ...}`, flowing through `derive_schema` → `MODE_SCHEMAS` → `title="..."` tooltips on mode-panel inputs both server-rendered (`render_schema_html`) and JS-rendered (`renderSchemaFieldJs`); (b) combinator kind dropdown gained `COMBINATOR_HELP` map + per-option tooltips; (c) visibility checkbox now cross-mount DOM-synced via `syncSiblingVisToggles` and clarified tooltip ("does not affect scoring") — kept as plot-only per user pick A; (d) range bounds labeled "Lower/Upper bound (optional)" — kept scalar-only since time-varying achievable via windowed AND composition. **Phase 2 — multi-peak + plots**: (a) `DominantFrequencyConfig.n_peaks: int = 1` (default single-peak, backward-compat); `_compute_fft_spectrum` + `_find_top_n_peaks` (pure-numpy local-maxima, sorts by amplitude then frequency for predictable PRBS pairing); hard-fail with reason when fewer peaks detected than declared. Spectrum arrays capped at 512 bins embedded into diagnostics. (b) `MODE_PLOT_EDITORS['dominant-frequency']` mounts a Plotly spectrum subplot (ref solid blue + actual dotted red + peak markers + shaded ± rel-tolerance bands around each reference peak) in the editor slot on activate, per user choice of activated-only. (c) `MODE_PLOT_CONTRIBUTIONS['event-timing']` overlays vertical dashed gray lines at reference events + solid blue lines at actual events + shaded tolerance bands on the main trajectory plot; `_detectEvents` uses Modelica duplicate-time convention JS-side. (d) `FieldSpec.ui_min` / `ui_max` soft caps (threaded via metadata) — `n_peaks` capped at 10 in the UI but respects existing larger values (`max = max(ui_max, current_value)`). +6 Python multi-peak tests + 2 Playwright visibility tests. End-to-end verified: tooltips render, spectrum attached, event detection works. Test count: 736 → 744 (+8). Decision D74.

**Leaf-state persistence across structural re-renders + reset button (complete, 2026-04-23)**: D73. Hotfix surfaced by user immediately after D72 — live-edited leaf values reset to authored defaults when any structural edit triggered a re-render. Two pre-existing bugs on one code path: (1) `leafState` is path-keyed, but `rebuildPaths(WORKING_TREE, '/metrics')` rewrites every node's `.path` after structural edits — a leaf whose path shifted (e.g., `/metrics/children/1` → `/metrics/children/0` when an earlier sibling was removed) lost its state via orphaned key; (2) even when paths don't shift (change-kind), every re-render re-inserts `leaf.mode_controls_html` verbatim from the server-generated string with original `value="..."` attrs — DOM reset to authored values even though `leafState[path].params` still held the edit. Both visible to users only when something triggered a re-render, which was rare before #52 landed. Fix: new `migrateLeafStatePaths()` snapshots `[{leaf, oldPath}]` before `rebuildPaths`, then two-pass migrates (read+delete all old, write all new) — leaf object identity preserved through structural edits, so ref-based mapping works without node UUIDs; new `refreshLeafInputsFromState()` post-render pass walks every `.node-leaf` and sets `input.value` from `leafState[path].params[field]` (+ window-controls). Bundled: `↻` reset button per leaf restores `params` / `window` from `original_params` / `original_window` captured at init/append time; reset does NOT flip `structureDirty` — value revert, not structural. +10 Playwright tests covering sibling-remove-no-shift, earlier-sibling-remove-shift, wrap-deepening, unwrap-shallowing, change-kind-no-shift-rerender, window-edit survives, reset button rendering, reset restores params, reset restores window, reset doesn't dirty structure. Test count: 726 → 736 (+10). Decision D73.

**Wrap-in-combinator + combinator-kind editing in reporter (complete, 2026-04-23)**: D72. Closes the one remaining A-tier reporter-as-IDE debt — every MetricTree authoring op is now clickable. Three mutation helpers on `WORKING_TREE`: `wrapWorkingNode(path, kind)` always produces a single-child parent (`kind(target)`) regardless of target shape, including root-wrap; `unwrapWorkingNode(path)` replaces a single-child combinator with its lone child; `changeCombinatorKind(path, newKind)` flips the kind field with seeded defaults for k-of-n (`k=max(1,n-1)`) / weighted (`weights=[1]*n`, `threshold=1.0`, `direction="less"`). Change-to-warn rejected on multi-child (user should wrap-in-warn instead — wrap *always* produces a 1-child parent, so warn is happy). UI: kind `<select>` dropdown in combinator header with 5 options; inline k / threshold / direction / weights[] inputs rendered when applicable; ⊕ wrap button on every node (leaves + combinators + root); ⊖ unwrap button only on 1-child combinators; ⊕ opens inline popup (same pattern as remove-confirm) with kind dropdown + Confirm / Cancel + ESC + click-away dismiss. No new patch op types — structural edits continue to emit wholesale `/metrics` replace. D66 validator at patch-apply time catches invalid tree states (≥ 1 primary leaf outside warn) — no live halos in v1. +17 Playwright tests covering every flow (kind dropdown enumeration, each kind transition, seeded defaults, wrap leaf / combinator / root, unwrap round-trip, multi-child warn rejection, per-variable cross-mount re-render, wholesale patch envelope). End-to-end smoke on `ModelicaTestingLib` — all 10 tests still PASS. Test count: 674 → 726 (+52 raw, +17 author-intended #52 tests plus previously-skipped-Playwright tests now running). Decision D72.

**Feature-showcase tests + NB overlay parity + simulate-only render fix (complete, 2026-04-23)**: D71. Grab-bag fill-in after D70. (1) Four feature-showcase tests added to ModelicaTestingLib — `TubeToleranceTest`, `FrequencyTest`, `MetricTreeTest`, `RangeCheckTest` — numerically valid and HTML-verified per-variable panels for every mode. (2) `resolve_mode` for range now accepts `min` / `max` shorthand alongside canonical `min_value` / `max_value`. (3) Sibling-backend overlay picker UI now renders on the no-baseline code path (was baseline-only); shared `setOverlayVisible(plotId, role, name, visible)` helper + generalized `wireOverlayPickers`. (4) `simulate_only` + no-baseline rendering fix — three bugs on one code path where a simulate-only test with no stored baseline showed "Simulation failed" in the per-test HTML and NO_REF on the index. Fixed in `compare_all` (dispatch to `compare_test` with `reference={}`), `_build_template_context` (skip the `sim_failed` heuristic for simulate-only), and the index status classifier (check `simulate_only` before `has_reference`). +3 regression tests. End-to-end validation: all 10 ModelicaTestingLib OM tests (including `SimulateOnlyTest` without baseline) now show PASS. Decision D71.

**Persistent-worker OpenModelica via OMPython (complete, 2026-04-22)**: D70. Follow-up explicitly deferred in D69. `PersistentOpenModelicaRunner(OpenModelicaRunner)` + `OpenModelicaWorker` — each worker holds one long-lived `OMCSessionZMQ` so MSL + library + deps load once per worker rather than once per test. Queue + dispatch-thread-per-worker mirrors `PersistentDymolaRunner` (watchdog, psutil hard-kill on timeout, disk-fallback, up-to-3 worker restarts). OMPython is an optional extra (`om = ["OMPython>=3.5"]`), not a hard dep: CLI's `_get_runner(persistent=True)` lazy-probes via `load_omc_session()` and falls back to `--batch` on RuntimeError with a stderr notice — same shape as Dymola's Python-interface fallback. Phase labels are cosmetic (OM `simulate()` bundles build+run in one `sendExpression` call with no mid-call progress hook, unlike Dymola's `translateModel`+`simulateModel` split); per-phase wall timings in the final report remain accurate since they come from the returned `SimulationResult` record's `timeFrontend`/…/`timeSimulation` fields. `_parsed_from_record` bypasses `log_parser.parse_omc_stdout` — OMPython already returns the record as a dict. Synthetic `omc_stdout.txt` artifact (header + simulate expr + pretty-printed record + `getErrorString()` output) keeps report parity with batch mode. `build_simulate_args` extracted from `build_simulate_mos` for DRY across both paths. Auto-inject MSL matches batch heuristic. PID tracked via `session.getpid()` for psutil kill. No startup-lock patching / stderr-noise suppression needed (OMPython is quieter than Dymola's embedded Java). Validation: ModelicaTestingLib smoke (6 tests, parallel=2) — persistent = 12s wall / 11s total work, batch = 16s wall / 24s total work; total-work ratio 0.46× (library-load amortized across workers). TRANSFORM's 326-test compile-dominated sweep is the intended beneficiary; not re-run this session. Test count: 679 → 706 (+27: 20 unit mocked `FakeSession` + 4 CLI wiring + 1 real-OMPython integration + helpers). Decision D70.

**OpenModelica backend (complete, 2026-04-22)**: D69. Third `SimulatorRunner` alongside Dymola + FMPy — first non-Dymola path that runs from Modelica source (FMPy consumes prebuilt FMUs). MVP uses `omc` as a subprocess driven by generated `.mos` scripts, analogous to Dymola's batch fallback; persistent-worker (OMPython / `OMCSessionZMQ`) + FMU export (`buildModelFMU`) deferred follow-ups. `mat_reader.py` hoisted from `simulators/dymola/` to `simulators/common/` and renamed `read_result_mat` — OM's `.mat` is DSresult-compatible by design, zero new reader code needed (also fixed a latent transpose bug in that reader: the `shape[0] < shape[1]` heuristic silently failed for MATs with ≤ 4 vars — all Dymola fixtures exceed that, OM's tight `variableFilter` exposed it). `OpenModelicaRunner` declares only `Capability.BATCH_FALLBACK`; artifact list fully static (`result_res.mat`, `result.log`, `result_info.json`, `simulate.mos`, `omc_stdout.txt`) via `fileNamePrefix="result"` on `simulate(...)`. Per-phase timings parsed from the REPL-echoed `record SimulationResult … end SimulationResult;` block (the `.mos` emits a bare `simulate(...)`; an earlier sentinel+`print()` design proved silently unreliable because `res` wasn't in scope for follow-up prints). `variableFilter` regex anchors `^(...)$`, escapes per-name, expands globs via `_pattern_to_regex`, and includes `unitTests.x[i]` + diagnostics explicitly — MSL is auto-injected into `dependencies` so empty-deps `testing.json` works across both backends. `Config.dependencies` classification: bare names ⇒ `loadModel(Name)`, path-like ⇒ `loadFile(path)`. MSL installed once per machine via `omc -e 'updatePackageIndex(); installPackage(Modelica); getErrorString();'`. Unified `testing.json` with auto-detect: `_auto_detect_simulator` iterates the `simulators` map in insertion order and picks the first entry whose binary resolves (respecting pinned OS-specific absolute paths — Windows `C:\…` detected via regex since PosixPath `is_absolute()` misses them on Linux); CLI `--simulator` and explicit `"simulator"` key still override. Also added `BACKEND_BINARY_NAMES` map so fallback PATH lookups use `omc` (not `openmodelica`). Validation: end-to-end sweep against `examples/modelica/ModelicaTestingLib/` on Linux — 5 primary baselines under `ReferenceResults/OpenModelica/linux/` + HTML report renders with per-phase timings (5 pass / 0 fail / 1 no-ref for SimulateOnlyTest). Tests: `test_openmodelica_mos.py` (17), `test_openmodelica_log_parser.py` (8), `test_openmodelica_runner.py` (6 unit + 4 integration, omc-gated), plus 7 new auto-detect tests in `test_config.py`. Test count: 637 → 679. Decision D69.

## Project Structure

```
ModelicaTesting/
├── src/
│   └── modelica_testing/        # Python package (src layout)
│       ├── cli.py               # CLI: discover, run, compare, export, manifest, add, spec-update
│       ├── config.py            # Config dataclass, path resolution, testing.json loading
│       ├── discovery/           # Test discovery: scan .mo for UnitTests, parse test_spec.json
│       ├── simulators/          # Abstract runner + Dymola backend (batch .mos, .mat reader, dslog parser)
│       ├── comparison/          # NRMSE and tube comparison with piecewise event handling
│       ├── storage/             # JSON reference storage with in-memory index
│       └── reporting/           # Console, JUnit XML, HTML reporters, plot generation
│           └── templates/       # Jinja2 templates (comparison.html, interactive.html) + comparison_data.json sidecar
├── examples/
│   └── modelica/
│       └── ModelicaTestingLib/  # Modelica library: UnitTests component + example models
│   ├── Components/UnitTests.mo  # Reusable UnitTests component for tracking variables
│   ├── Examples/                # SimpleTest, EventTest, ConstantTest, IntervalTest, NoUnitTest
│   └── Resources/ReferenceResults/  # testing.json + reference baselines for this library
├── tests/                       # pytest test suite (174 tests)
│   ├── fixtures/                # Test data: dslog.txt, .mat file, test_spec.json
│   └── test_*.py                # Comparator, config, discovery, storage, simulators
├── docs/                        # Design decisions, patterns, architecture, constraints, usage
├── pyproject.toml               # uv/hatchling project config
└── CLAUDE.md
```

## Running the Tool

The package ships a console script (`[project.scripts]` in `pyproject.toml`). The canonical dev invocation is `uv run modelica-testing ...`. `python -m modelica_testing ...` is supported as a fallback (both call `cli.main_entry`). End users install via `uv tool install modelica-testing` (or `pipx install`) and run plain `modelica-testing`.

```bash
# With testing.json containing source_path — single entry point
uv run modelica-testing --config path/to/testing.json run

# Or with explicit flags
uv run modelica-testing --source-path /path/to/MyLib --reference-root /path/to/refs run

# Interactive review (accept/skip/plot per test)
uv run modelica-testing --config testing.json run -i

# Interactive review filtered to specific categories
uv run modelica-testing --config testing.json run -i failed
uv run modelica-testing --config testing.json run -i no-baseline
# Categories: failed, no-baseline, warnings, sim-failed, passed, all

# Accept all results as new baselines
uv run modelica-testing --config testing.json run --accept

# Generate HTML report with per-test plots (static + interactive Plotly)
uv run modelica-testing --config testing.json run --report ./reports

# Parallel run with small-batch queue dispatch (better load balancing + crash isolation)
uv run modelica-testing --config testing.json run --parallel 4 --batch-size 3
# Live progress: open work_dir/dashboard.html (auto-refreshes every 2s; URL printed on start)

# Filter accepts: glob, comma-separated list, or @file (one pattern per line, # comments)
uv run modelica-testing --config testing.json run --filter "Foo.A,Foo.B"
uv run modelica-testing --config testing.json run --filter @rerun.txt

# Incremental rerun + full merged report (rerun a subset, report covers everything)
uv run modelica-testing --config testing.json run --filter @failed.txt --merge --report

# Auto-rerun previously failed tests (implies --merge)
uv run modelica-testing --config testing.json run --rerun --report
# Or pick categories: failed, no-baseline, warnings, sim-failed, passed
uv run modelica-testing --config testing.json run --rerun failed,sim-failed --report

# Compare without re-running simulations (uses last results)
uv run modelica-testing --config testing.json compare

# Apply tolerance config exported from interactive report to test_spec.json
uv run modelica-testing --config testing.json spec-update tolerance_config.json

# Dump reference manifest (ref ID to model name mapping) without running tests
uv run modelica-testing --config testing.json manifest dump

# Prune orphan manifest entries (models no longer in discovery — dry-run by default)
uv run modelica-testing --config testing.json manifest cleanup --orphans
uv run modelica-testing --config testing.json manifest cleanup --orphans --apply

# Persistent workers via the Dymola Python interface are the DEFAULT.
# Live per-test dashboard (with phase: translating / simulating / finalizing),
# natural load balancing, per-test timeout watchdog with disk-check rescue,
# per-phase timing in per-test report ("Timing" section) and on index (sortable columns).
uv run modelica-testing --config testing.json run --parallel 4 --report

# Force the legacy batched .mos runner (e.g., for environments without the Python interface)
uv run modelica-testing --config testing.json run --batch --parallel 4 --batch-size 3 --report

# Diagnose discovery of the Dymola Python interface archive
uv run modelica-testing check-dymola
```

## Running Tests

```bash
uv pip install -e ".[dev]"    # One-time: install package + pytest
uv run pytest                  # Run the test suite
```

## Configuration

The tool looks for `testing.json` near the library root or reference root. Key fields:

- `source_path` — path to library's source location (Modelica package.mo directory, FMU directory, ...; relative to testing.json)
- `simulator` — named entry like `"Dymola"` or `"Dymola 2025"`
- `simulators` — map of simulator names to candidate executable paths
- `simulator_setup` — list of Modelica commands run after library loading (user-specific settings)
- `dependencies` — paths to dependency library roots loaded before simulation
- `reference_root` — where reference results live (default: `<repo>/Resources/ReferenceResults`)
- `test_spec` — path to external test definitions file
- `tolerance` — global NRMSE comparison tolerance (default: `1e-4`)
- `diagnostic_variables` — variables auto-captured but not compared (default: `["CPUtime", "EventCounter"]`)

Note: `OutputCPUtime := true;` and `Advanced.UI.TranslationInCommandLog := true;` are hardcoded in the Dymola runner — no need to add them to `simulator_setup`.

Reference results are partitioned by `<reference_root>/<SimulatorBackend>/<os>/`.

### test_spec.json format

Simulation parameters live under a `simulation` key, comparison settings under a `comparison` key. Both are optional — minimal entries need only `model` and `variables`:

```json
{
  "tests": [
    {
      "model": "MyLib.Examples.SimpleTest",
      "variables": ["pipe.T[1]", "pipe.m_flow"],
      "simulation": {"stop_time": 100, "tolerance": 1e-6},
      "comparison": {
        "tolerance": 0.01,
        "variable_overrides": {
          "pipe.T[1]": {"tolerance": 0.1},
          "pipe.p[1]": {"mode": "tube", "tube_width_mode": "rel", "tube_rel": 0.02}
        }
      }
    }
  ]
}
```

### Comparison modes

**NRMSE** (default): `NRMSE = RMSE / signal_range`. Pass if below tolerance.

**Tube**: envelope around the reference trajectory. Configured per-variable via `variable_overrides` with `"mode": "tube"`. Three width modes controlled by `tube_width_mode`:
- `"rel"` (default in interactive UI): width = fraction of |reference| (e.g., `"tube_rel": 0.02` = 2%)
- `"band"` (or legacy `"abs"`): width = offset in signal units (e.g., `"tube_abs": 500`)
- `"absolute"`: upper/lower are literal y-axis values (not offsets from reference)

Legacy format (no `tube_width_mode`): width = `max(tube_abs, tube_rel * |reference|)`. Pass if every point stays inside the tube. Supports time-varying tubes via `tube_points` with linear or stepwise interpolation.

### Tolerance resolution order

Per-variable override (spec) > per-variable override (reference JSON) > per-test comparison tolerance > reference JSON comparison tolerance > config.tolerance > default (1e-4). When accepting results, comparison settings are saved in the reference JSON's `comparison` section so tolerances travel with the baseline.


## Key Abstractions

- **`Config`** (`config.py`) — resolves all paths from CLI args + `testing.json` + defaults; passed to runners and reporters but not to comparison functions
- **`TestModel`** (`discovery/test_registry.py`) — fully resolved test with model ID, simulation params, tracked variables, source
- **`SimulatorRunner`** (`simulators/base.py`) — abstract interface; backends self-register via `@register` decorator; `get_runner(config)` factory in `simulators/__init__.py`
- **`DymolaRunner`** (`simulators/dymola/runner.py`) — batch execution backend; `DymolaConfig` dataclass extracts Dymola-specific settings from Config
- **`ReferenceStore`** (`storage/reference_store.py`) — CRUD for per-test JSON reference files; `RefIndex` built in-memory from scanning ref files
- **`ComparisonMode`** (`comparison/modes.py`) — strategy pattern for variable comparison: `NrmseMode`, `TubeMode`, `FinalOnlyMode` with typed config dataclasses; `resolve_mode()` factory builds mode from per-variable override dict
- **`comparator`** (`comparison/comparator.py`) — orchestrates per-test comparison; `compare_test()` takes `default_tolerance` and `final_only` (not Config); delegates per-variable comparison to `ComparisonMode` strategies

## Design Principles

1. **Library-agnostic**: auto-detects library name from `package.mo`, all paths configurable
2. **Simulator-agnostic**: Dymola-specific code isolated in `simulators/dymola/`; abstract `SimulatorRunner` interface with registry pattern
3. **Stable test IDs**: numeric IDs (`ref_0001.json`) with model ID inside each file; IDs never reused; in-memory index built by scanning ref files (no persistent manifest)
4. **Reference partitioning**: results split by simulator backend and OS since solvers produce platform-specific results
5. **Batch execution**: load libraries once per worker, run N tests, exit — avoids per-test startup overhead
6. **No backward compatibility**: clean breaks during development; migration utilities provided for format changes
7. **Strategy over conditionals**: comparison modes and simulator backends use strategy/registry patterns instead of if/elif dispatch
